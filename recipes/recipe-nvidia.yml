---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
# Wolf OS for NVIDIA graphics. Identical to recipe.yml except for the base image,
# which adds NVIDIA's driver (built and signed for every kernel by Universal Blue).
name: wolf-os-nvidia
description: Wolf OS (NVIDIA) - a hardened, rollback-safe everyday desktop with an isolated hacking lab.

base-image: ghcr.io/ublue-os/kinoite-nvidia
image-version: 44 # keep in sync with recipe.yml

modules:
  - from-file: common-modules.yml
